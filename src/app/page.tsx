export function redirectToDestination() {
  // fetch session information here
  // const session = { username: "test" };
  const session = null

  // if (!session) return redirect(LINK_SIGNUP)
  // redirect(LINK_HOME)
}

const Page = () => {
  redirectToDestination()
  return null
}

export default Page
